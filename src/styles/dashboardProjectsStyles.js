import { makeStyles } from "@material-ui/core";

export const useDashboardProjectsStyles = makeStyles(() => ({
    container: {
        width: "100%",
        marginTop: "15px"
    },
    cardListContainer: {
        display: "flex",
        flexWrap: "wrap"
    },
}))